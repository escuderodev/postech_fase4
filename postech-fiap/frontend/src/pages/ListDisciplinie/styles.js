import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#333333',
    padding: 32
  },
  title: {
    fontSize: 24,
    fontWeight: "bold",
    color: "#e5bf3c",
    marginBottom: 16,
}
});